{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        /* Runtime Environment */
        "lib": ["ESNext", "DOM", "DOM.Iterable"],
        "types": [
            "vite/client",
            "react",
            "react-dom",
            "node"
        ]
    }
}
